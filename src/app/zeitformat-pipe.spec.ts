import { ZeitformatPipe } from './zeitformat-pipe';

describe('ZeitformatPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeitformatPipe();
    expect(pipe).toBeTruthy();
  });
});
